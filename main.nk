fn calculate_fib(n: i32): i32 {

	pure n_minus_one: i32 = n - 1;
	pure n_minus_two: i32 = n - 2;
	
	if compare.is_less_than_or_equals(n, 1) {
		return load n;
	};

	return calculate_fib(n_minus_one) + calculate_fib(n_minus_two);
}

fn main(): i32 {
	return calculate_fib(15);
}