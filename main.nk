fn add_two(): i32
{
	return 2;
}

fn main(): i32
{
	alloc variable_name: i32 = 1;
	
	load state1: i32 = variable_name;
	state1 += 1;

	load state2: i32 = variable_name;
	state2 += 2;
	state2 += state1;

	load state3: i32 = variable_name;
	state3 += 3;
	state3 += state2 + state1;

	return load state3;
}